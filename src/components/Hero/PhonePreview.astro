---

---

<div class="phone-mockup animate-slide-in-right">
  <div class="phone-notch"></div>
  <div class="phone-screen">
    <div class="status-bar">
      <span class="time">9:41</span>
      <div class="status-icons">
        <span class="signal">📶</span>
        <span class="wifi">📶</span>
        <span class="battery">🔋</span>
      </div>
    </div>
    <div class="app-header">
      <div class="header-left">
        <span class="app-name">ExpenseAI</span>
        <span class="date">Today, July 3</span>
      </div>
      <div class="profile-icon">👤</div>
    </div>
    <div class="balance-card">
      <p class="balance-label">Monthly Budget</p>
      <p class="balance-amount">$2,847.50</p>
      <div class="balance-bar">
        <div class="balance-progress"></div>
      </div>
      <p class="balance-remaining">$652.50 remaining</p>
    </div>
    <div class="section-header">
      <h3>Recent Transactions</h3>
      <span class="see-all">See all</span>
    </div>
    <div class="expense-item">
      <div class="expense-icon coffee"></div>
      <div class="expense-details">
        <span class="expense-name">Starbucks Coffee</span>
        <span class="expense-category">Food & Dining • 2 hours ago</span>
      </div>
      <span class="expense-amount">-$12.50</span>
    </div>
    <div class="expense-item">
      <div class="expense-icon transport"></div>
      <div class="expense-details">
        <span class="expense-name">Uber to Downtown</span>
        <span class="expense-category">Transportation • 4 hours ago</span>
      </div>
      <span class="expense-amount">-$18.75</span>
    </div>
    <div class="expense-item">
      <div class="expense-icon shopping"></div>
      <div class="expense-details">
        <span class="expense-name">Whole Foods Market</span>
        <span class="expense-category">Groceries • Yesterday</span>
      </div>
      <span class="expense-amount">-$45.20</span>
    </div>
  </div>
  <div class="home-indicator"></div>
</div>

<style>
  .phone-mockup {
    background: linear-gradient(145deg, #1a1a1a 0%, #2d2d2d 100%);
    border-radius: 28px;
    padding: 0.5rem;
    box-shadow: 
      0 25px 50px rgba(0, 0, 0, 0.25),
      0 0 0 1px rgba(255, 255, 255, 0.1);
    max-width: 340px;
    position: relative;
    z-index: 2;
    transform: translateX(120px);
  }

  .animate-slide-in-right {
    opacity: 0;
    transform: translateX(100px);
    animation: slideInRight 1s ease-out 0.6s forwards;
  }

  @keyframes slideInRight {
    to {
      opacity: 1;
      transform: translateX(120px);
    }
  }

  .phone-notch {
    position: absolute;
    top: 0.5rem;
    left: 50%;
    transform: translateX(-50%);
    width: 120px;
    height: 28px;
    background: #000;
    border-radius: 0 0 20px 20px;
    z-index: 10;
  }

  .home-indicator {
    position: absolute;
    bottom: 8px;
    left: 50%;
    transform: translateX(-50%);
    width: 140px;
    height: 4px;
    background: rgba(255, 255, 255, 0.3);
    border-radius: 2px;
  }

  .phone-screen {
    background: white;
    border-radius: 24px;
    padding: 0;
    min-height: 640px;
    overflow: hidden;
    position: relative;
  }

  .status-bar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0.75rem 1.5rem 0.5rem;
    font-size: 0.9rem;
    font-weight: 600;
    background: white;
    z-index: 5;
  }

  .time {
    color: #000;
  }

  .status-icons {
    display: flex;
    gap: 0.25rem;
    font-size: 0.8rem;
  }

  .app-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1rem 1.5rem;
    background: white;
  }

  .header-left {
    display: flex;
    flex-direction: column;
  }

  .app-name {
    font-weight: 700;
    color: #6366f1;
    font-size: 1.1rem;
  }

  .date {
    color: #666;
    font-size: 0.85rem;
    margin-top: 0.2rem;
  }

  .profile-icon {
    width: 32px;
    height: 32px;
    background: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1rem;
  }

  .balance-card {
    margin: 0 1.5rem 1.5rem;
    padding: 1.5rem;
    background: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
    border-radius: 16px;
    color: white;
  }

  .balance-label {
    font-size: 0.9rem;
    opacity: 0.9;
    margin-bottom: 0.5rem;
  }

  .balance-amount {
    font-size: 2rem;
    font-weight: 700;
    margin-bottom: 1rem;
  }

  .balance-bar {
    background: rgba(255, 255, 255, 0.3);
    height: 6px;
    border-radius: 3px;
    margin-bottom: 0.5rem;
    overflow: hidden;
  }

  .balance-progress {
    background: white;
    height: 100%;
    width: 75%;
    border-radius: 3px;
  }

  .balance-remaining {
    font-size: 0.9rem;
    opacity: 0.9;
  }

  .section-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0 1.5rem 1rem;
  }

  .section-header h3 {
    font-size: 1.1rem;
    font-weight: 600;
    color: #1f2937;
  }

  .see-all {
    color: #6366f1;
    font-size: 0.9rem;
    font-weight: 500;
  }

  .expense-item {
    display: flex;
    align-items: center;
    padding: 0.75rem 1.5rem;
    border-bottom: 1px solid #f3f4f6;
  }

  .expense-icon {
    width: 44px;
    height: 44px;
    border-radius: 12px;
    margin-right: 1rem;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 600;
    color: white;
    font-size: 1rem;
  }

  .expense-icon.coffee {
    background: #f59e0b;
  }

  .expense-icon.transport {
    background: #06b6d4;
  }

  .expense-icon.shopping {
    background: #10b981;
  }

  .expense-icon.coffee::after {
    content: "☕";
  }

  .expense-icon.transport::after {
    content: "🚗";
  }

  .expense-icon.shopping::after {
    content: "🛒";
  }

  .expense-details {
    flex: 1;
    display: flex;
    flex-direction: column;
  }

  .expense-name {
    font-weight: 600;
    margin-bottom: 0.2rem;
  }

  .expense-category {
    color: #666;
    font-size: 0.85rem;
  }

  .expense-amount {
    font-weight: 700;
    color: #1f2937;
    font-size: 1rem;
  }

  @media (max-width: 768px) {
    .phone-mockup {
      max-width: 260px;
      transform: translateX(0);
      box-shadow: 
        0 20px 40px rgba(0, 0, 0, 0.3),
        0 0 0 1px rgba(255, 255, 255, 0.15);
    }

    .animate-slide-in-right {
      animation: slideInRightMobile 1s ease-out 0.6s forwards;
    }

    @keyframes slideInRightMobile {
      to {
        opacity: 1;
        transform: translateX(0);
      }
    }
  }

  @media (max-width: 576px) {
    .phone-mockup {
      max-width: 240px;
      box-shadow: 
        0 15px 30px rgba(0, 0, 0, 0.35),
        0 0 0 1px rgba(255, 255, 255, 0.2);
    }

    .phone-screen {
      min-height: 560px;
    }

    .app-header {
      padding: 0.8rem 1.2rem;
    }

    .balance-card {
      margin: 0 1.2rem 1.2rem;
      padding: 1.2rem;
    }

    .balance-amount {
      font-size: 1.8rem;
    }

    .expense-item {
      padding: 0.6rem 1.2rem;
    }

    .expense-icon {
      width: 38px;
      height: 38px;
      margin-right: 0.8rem;
    }
  }

  @media (max-width: 480px) {
    .phone-mockup {
      max-width: 220px;
      box-shadow: 
        0 12px 25px rgba(0, 0, 0, 0.4),
        0 0 0 1px rgba(255, 255, 255, 0.25);
    }

    .phone-screen {
      min-height: 520px;
    }

    .status-bar {
      padding: 0.6rem 1.2rem 0.4rem;
      font-size: 0.8rem;
    }

    .app-header {
      padding: 0.7rem 1rem;
    }

    .app-name {
      font-size: 1rem;
    }

    .date {
      font-size: 0.8rem;
    }

    .balance-card {
      margin: 0 1rem 1rem;
      padding: 1rem;
    }

    .balance-amount {
      font-size: 1.6rem;
    }

    .expense-item {
      padding: 0.5rem 1rem;
    }

    .expense-icon {
      width: 36px;
      height: 36px;
      margin-right: 0.7rem;
    }

    .expense-name {
      font-size: 0.9rem;
    }

    .expense-category {
      font-size: 0.8rem;
    }

    .expense-amount {
      font-size: 0.9rem;
    }
  }
</style>
